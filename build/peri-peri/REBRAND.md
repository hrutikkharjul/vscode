# PeriPeri — VS Code rebrand

This folder contains the brand assets and build helpers for **PeriPeri**, a
chili-pepper themed rebrand of the VS Code source tree.

## What was changed

### Branding (`product.json`)

| Field | Before | After |
|---|---|---|
| `nameShort` / `nameLong` | `Code - OSS` | `PeriPeri` |
| `applicationName` | `code-oss` | `periperi` |
| `dataFolderName` | `.vscode-oss` | `.periperi` |
| `sharedDataFolderName` | `.vscode-oss-shared` | `.periperi-shared` |
| `serverApplicationName` | `code-server-oss` | `periperi-server` |
| `serverDataFolderName` | `.vscode-server-oss` | `.periperi-server` |
| `tunnelApplicationName` | `code-tunnel-oss` | `periperi-tunnel` |
| `win32MutexName` | `vscodeoss` | `periperi` |
| `win32DirName` / `win32NameVersion` | `Microsoft Code OSS` | `PeriPeri` |
| `win32RegValueName` | `CodeOSS` | `PeriPeri` |
| `win32AppUserModelId` | `Microsoft.CodeOSS` | `PeriPeri.PeriPeri` |
| `win32ShellNameShort` | `C&ode - OSS` | `&PeriPeri` |
| `win32TunnelMutex` / `win32TunnelServiceMutex` | `vscodeoss-…` | `periperi-…` |
| `darwinBundleIdentifier` | `com.visualstudio.code.oss` | `com.periperi.app` |
| `linuxIconName` | `code-oss` | `periperi` |
| `urlProtocol` | `code-oss` | `periperi` |
| `win32x64AppId` | `D77B7E06-…` | new GUID |
| `win32arm64AppId` | `D1ACE434-…` | new GUID |
| `win32x64UserAppId` | `CC6B787D-…` | new GUID |
| `win32arm64UserAppId` | `3AEBF0C8-…` | new GUID |
| `darwinProfileUUID` / `darwinProfilePayloadUUID` | (Microsoft) | new UUIDs |

> **Why fresh GUIDs?** Inno Setup uses `AppId` to identify a product on the
> system. Reusing VS Code's GUIDs would make the PeriPeri installer claim
> ownership of an existing VS Code install and overwrite/uninstall it.

### Branding (`package.json`)

| Field | Before | After |
|---|---|---|
| `name` | `code-oss-dev` | `periperi` |
| `author.name` | `Microsoft Corporation` | `PeriPeri` |

### Icons (`resources/win32/`)

Replaced with a chili-pepper logo generated by `make_logo.py`:

- `code.ico` — multi-resolution ICO containing 16 / 24 / 32 / 48 / 64 / 128 / 256 px frames
- `code_70x70.png` — Windows small tile
- `code_150x150.png` — Windows medium tile

Originals are preserved in `build/peri-peri/backup/win32/`.

### Logo source

- `make_logo.py` — Pillow script that draws the pepper procedurally
- `chili.svg` — hand-written SVG mirror of the bitmap shape
- `chili_1024.png` — master render at 1024×1024

To regenerate after editing `make_logo.py`:

```powershell
python build\peri-peri\make_logo.py
```

## Building the installer

The PowerShell wrapper drives the same gulp pipeline VS Code uses on Azure
Pipelines, minus the code-signing steps.

```powershell
pwsh -ExecutionPolicy Bypass -File build\peri-peri\build-installer.ps1
```

### Prerequisites

| Tool | Version | Notes |
|---|---|---|
| Node.js | 22.x (`.nvmrc` says 22.22.1) | currently `v22.13.1` on this machine — close enough |
| npm | 10.x | bundled with Node |
| Python | 3.x on PATH | for native module gyp builds |
| Visual Studio Build Tools 2022 | "Desktop development with C++" workload | required for `kerberos`, `native-keymap`, `node-pty`, `@vscode/spdlog` |
| Free disk | ~10 GB | for `node_modules` + build artefacts |

Inno Setup itself is **bundled as the npm package `innosetup`** (see
`devDependencies` in `package.json`). You do not need to install it
separately — `npm install` puts `ISCC.exe` under
`node_modules/innosetup/bin/`.

### ⚠️ Required VS components on this machine

A real end-to-end build was completed on this machine on 2026-05-22. The
issues encountered and their fixes are captured below — `build-installer.ps1`
encapsulates them. Some of these are environment-specific.

#### 1. Node.js version

The repo's `.nvmrc` pins **22.22.1**. Older minor versions (e.g. 22.13.1) are
rejected by `build/npm/preinstall.ts` and also fail later because some deps
require ≥22.14 (`copilot-chat`) or ≥22.15 (`@vscode/proxy-agent`). The build
script handles this two ways:

- Sets `VSCODE_SKIP_NODE_VERSION_CHECK=1` and `NODE_OPTIONS=--experimental-strip-types`
  so older Node 22.x can run the `.ts` build scripts.
- Strongly recommended: `nvm install 22.22.1 && nvm use 22.22.1`.

#### 2. node-gyp can't find Visual Studio

node-gyp 11 (bundled with npm 10) uses a PowerShell call to `vswhere.exe`
that fails on this machine, even though VS Build Tools 2022 with the C++
workload is installed. **Fix:** run all `npm install` / `npm rebuild`
commands inside a `vcvars64.bat` environment so `VCINSTALLDIR` is set —
node-gyp then trusts the env var directly. The build script does this
automatically via `RunUnderVcvars`.

#### 3. Spectre-mitigated MSVC libs

VS Code's native modules (`@vscode/native-watchdog`, `@vscode/sqlite3`,
`kerberos`, `node-pty`, …) declare `'SpectreMitigation': 'Spectre'` in
their `binding.gyp`. The matching MSVC runtime is an Individual Component
in Visual Studio Build Tools that is **not** installed by the default C++
workload.

Two ways to satisfy it:

- **Proper:** Visual Studio Installer → Modify the 2022 Build Tools install
  → Individual components → check
  *"MSVC v143 - VS 2022 C++ x64/x86 Spectre-mitigated libs (Latest)"*.
- **Workaround:** run `build/peri-peri/disable-spectre.ps1` to rewrite the
  Spectre setting to `false` across all `*.gyp` / `*.gypi` files in
  `node_modules/` and `remote/node_modules/`. Produces native binaries that
  are NOT hardened against Spectre — fine for a personal/dev installer,
  not appropriate for a public release.

> **Note on iteration:** because the Spectre values are baked into files
> inside `node_modules/`, every fresh `npm install` re-extracts them and
> the patcher must run again. The recommended order is:
> 1. `npm install --ignore-scripts`
> 2. `npm install --ignore-scripts` inside `remote/`
> 3. `disable-spectre.ps1`
> 4. `npm rebuild`
> 5. `npm install` (this time letting lifecycle scripts run — they will
>    see the patched gyp files and compile successfully)

#### 4. Windows SDK signtool on PATH

`gulpfile.vscode.ts` calls `signtool.exe` to verify/strip Authenticode
signatures before `rcedit` patches them. The Build Tools install includes
the Windows SDK bin dir, but it isn't on PATH by default. The build script
prepends:

```
C:\Program Files (x86)\Windows Kits\10\bin\10.0.22621.0\x64
```

#### 5. Cross-platform binaries inside copilot extension

The bundled `extensions/copilot` ships `.node` files for many platforms
(macOS, Linux x64, Linux ARM, Raspberry Pi, etc.). The default
`patchWin32DependenciesTask` in `build/gulpfile.vscode.ts` calls `rcedit`
on every `*.node` file it finds, which fails for non-Windows binaries.

Fix applied to `build/gulpfile.vscode.ts`: wrap the `rcedit`/`signtool`
call in try/catch and emit a warning rather than throw. See the diff in
`patchWin32DependenciesTask`. The skipped binaries don't run on Windows
anyway, so leaving them un-patched is harmless.

#### 6. compile-copilot-extension-build is not part of compile-extensions-build

The umbrella `compile-extensions-build` task that `vscode-win32-x64-min-ci`
depends on does **not** include the local copilot extension. Without
copilot in `.build/extensions/`, the post-package `prepareBuiltInCopilotRipgrepShim`
step throws. Run `npm run gulp compile-copilot-extension-build` separately
before `vscode-win32-x64-min-ci`. The build script does this.

### Pipeline stages (what the script runs)

1. `npm install` — pulls `innosetup`, `electron`, `rcedit`, native modules
2. `npm run gulp core-ci` — compiles, mangles, minifies the core sources
3. `npm run gulp vscode-win32-x64-min-ci` — packages Electron + sources into
   `..\VSCode-win32-x64\` (sibling of the repo)
4. `npm run gulp vscode-win32-x64-inno-updater` — copies `inno_updater.exe`
   and rewrites its embedded icon with `code.ico`
5. `npm run gulp vscode-win32-x64-system-setup` — invokes `ISCC.exe` against
   `build/win32/code.iss` to produce the installer

### Output

The signed Azure pipeline names the installer using `OutputBaseFilename` from
`code.iss`. Without signing, you'll find the unsigned `.exe` in:

```
.build\win32-x64\system-setup\
```

For a per-user installer, run with `-Target user` — the output goes to
`.build\win32-x64\user-setup\`.

### Common failures

- **`gyp ERR! find VS`** during `npm install`
  → Install Visual Studio Build Tools 2022 with the C++ workload.

- **`ENOENT: spawn ISCC.exe`** during the setup task
  → `npm install` did not complete; re-run it. `node_modules/innosetup`
  must exist.

- **`Out of memory`** during compile
  → The gulpfile already passes `--max-old-space-size=8192`. If it still
  OOMs, close other apps; the build wants ~6 GB free RAM.

- **`Cannot find module 'X'`** after a partial `npm install`
  → Delete `node_modules` and re-run `npm install` from a clean state.

## Reverting

To restore the original VS Code branding and icons:

```powershell
Copy-Item build\peri-peri\backup\product.json.bak product.json -Force
Copy-Item build\peri-peri\backup\package.json.bak package.json -Force
Copy-Item build\peri-peri\backup\win32\code.ico.bak resources\win32\code.ico -Force
Copy-Item build\peri-peri\backup\win32\code_70x70.png.bak resources\win32\code_70x70.png -Force
Copy-Item build\peri-peri\backup\win32\code_150x150.png.bak resources\win32\code_150x150.png -Force
```
